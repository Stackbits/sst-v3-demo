export const ERROR_CODES = Object.freeze({
    '400.1': 4001,
});

export const ERROR_SHORT_MSGS = Object.freeze({
    '400.1_ERROR': 'INVALID_INPUT',
});

export const ERROR_LONG_MSGS = Object.freeze({
    '400.1_ERROR': 'Invalid Request Input',
});
